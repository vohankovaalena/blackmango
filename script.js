/* ===========================
   TRANSLATIONS
   =========================== */
const translations = {
    cz: {
        'nav.services': 'Služby',
        'nav.portfolio': 'Portfolio',
        'nav.about': 'O nás',
        'nav.references': 'Reference',
        'nav.faq': 'FAQ',
        'nav.contact': 'Kontakt',
        'nav.hamburger.aria': 'Otevřít menu',
        'hero.subtitle': 'Kreativní studio',
        'hero.text': 'Za každou úspěšnou značkou stojí promyšlený design, dobrý obsah a jasná strategie. A od toho jsme tady my.',
        'hero.cta': 'Domluvit konzultaci',
        'partners.title': 'Spolupracujeme',
        'services.heading': 'Naše služby',
        'services.hint.desktop': 'Najeďte myší na kartu pro více informací',
        'services.hint.mobile': 'Klikněte na kartu pro více informací',
        'card1.aria': 'Design a digitální prostředí',
        'card1.heading': 'Design a digitální prostředí',
        'card1.question': 'Jak mít web a materiály, které budou působit profesionálně a přehledně?',
        'peel.label': 'klikněte<br>na kartu',
        'card1.item1': 'Návrh a tvorba webových stránek',
        'card1.item2': 'Grafika pro sociální sítě',
        'card1.item3': 'Tiskové materiály',
        'card1.item4': 'Prezentace & šablony',
        'card2.aria': 'Marketing & strategie',
        'card2.heading': 'Marketing & strategie',
        'card2.question': 'Jak efektivně získávat nové zákazníky online?',
        'card2.item1': 'Strategie komunikace',
        'card2.item2': 'Správa sociálních sítí',
        'card2.item3': 'Placená online reklama',
        'card3.aria': 'Konzultace, mentoring & vzdělávání',
        'card3.heading': 'Konzultace, mentoring & vzdělávání',
        'card3.question': 'Chcete mít svůj marketing pod kontrolou? Naučíme Vás přesně to, co potřebujete.',
        'card3.item1': 'Individuální konzultace na míru',
        'card3.item2': 'Kurzy zaměřené na práci se sociálními sítěmi',
        'card4.aria': 'Značka & identita',
        'card4.heading': 'Značka & identita',
        'card4.question': 'Jak vybudovat značku, která je snadno rozpoznatelná a důvěryhodná?',
        'card4.item1': 'Branding a rebranding',
        'card4.item2': 'Tvorba loga',
        'card4.item3': 'Sjednocení vizuální identity',
        'portfolio.heading': 'Portfolio',
        'portfolio.subtitle': 'Vybrané projekty, na nichž jsme pracovali',
        'portfolio.branding.title': 'Značky, kterým jsme vytvořily vizuální identitu',
        'portfolio.prev.aria': 'Předchozí',
        'portfolio.next.aria': 'Další',
        'portfolio.pdf1.aria': 'Zobrazit náhled PDF portfolio 1',
        'portfolio.pdf2.aria': 'Zobrazit náhled PDF portfolio 2',
        'portfolio.pdf3.aria': 'Zobrazit náhled PDF portfolio 3',
        'portfolio.grafika.title': 'Digitální grafika a sociální sítě',
        'portfolio.grafika.aria': 'Ukázky digitální grafiky',
        'portfolio.web.title': 'Weby, které díky nám dostaly šmrnc',
        'portfolio.web.aria': 'Ukázky webů z portfolia',
        'portfolio.web.prev.aria': 'Předchozí web',
        'portfolio.web.next.aria': 'Další web',
        'portfolio.print.title': 'Tiskoviny',
        'portfolio.print.aria': 'Ukázky tiskovin',
        'print.label1': 'Nápojový lístek – La Familia',
        'print.label2': 'B4 Fashion – design balíčků',
        'print.label3': 'Svatební pozvánka',
        'print.label4': 'Zpravodaj obce Dubno',
        'print.label5': 'Dárkový poukaz – Fyzio Body Profik',
        'print.label6': 'Vizitka – P2 Projekt',
        'print.btn1.aria': 'Zobrazit nápojový lístek La Familia',
        'print.btn2.aria': 'Zobrazit B4 Fashion – design balíčků',
        'print.btn3.aria': 'Zobrazit svatební pozvánku',
        'print.btn4.aria': 'Zobrazit zpravodaj obce Dubno',
        'print.btn5.aria': 'Zobrazit dárkový poukaz Fyzio Body Profik',
        'print.btn6.aria': 'Zobrazit vizitku P2 Projekt',
        'lightbox.aria': 'Náhled obrázku',
        'lightbox.close.aria': 'Zavřít náhled',
        'about.heading': 'O nás',
        'about.p1': 'Black Mango vzniklo z potřeby <strong>dělat věci jinak</strong>. Spojily jsme zkušenosti z různých oblastí a vytvořily prostor, kde <strong>grafika, obsah a přístup ke klientovi</strong> jdou ruku v ruce.',
        'about.p2': 'Ke každému projektu přistupujeme <strong>individuálně</strong>. Chceme pochopit, co skutečně potřebujete, a pak to zpracovat pečlivě a s rozvahou. <strong>Projekt vedeme my</strong>, hlídáme termíny a to, co si domluvíme, platí. <strong>Vy se můžete soustředit na svůj byznys.</strong>',
        'about.p3': 'Dobrá spolupráce podle nás funguje oběma směry. Naši práci přizpůsobujeme vašim potřebám, ale když nám něco nedává smysl, <strong>řekneme to na rovinu</strong>. Ne proto, abychom měli pravdu, ale proto, že <strong>nám záleží na výsledku.</strong> Pro naše klienty jsme <strong>STRATEGICKÝM PARTNEREM</strong>.',
        'about.p4': '<strong>PŘINÁŠÍME NĚCO NAVÍC.</strong> Provázíme vás od prvního nápadu až po výsledek, který vidíte. Proplacená faktura pro nás není tečka za spoluprací. <strong>Fandíme svým klientům dál, sledujeme, jak se jim daří, a jsme tu, když potřebují poradit nebo posunout věci o krok dál.</strong>',
        'about.value1': '9 let zkušeností',
        'about.value2': 'individuální přístup',
        'about.value3': 'dlouhodobé partnerství',
        'about.value4': 'dodržování termínů',
        'references.heading': 'Reference',
        'testimonial1': '„Spolupráce s Black Mango je opravdu skvělá. Již při prvním setkání jsem věděla, že naše propojení bude přínosné. Díky jejich schopnostem a dovednostem, flexibilitě, pohotovosti a zaujetím vznikají na mých sociálních sítích příspěvky, které oslovují klienty. Současně tak vzniká prostor pro další projekty a spolupráci. Jejich tvorba mi velmi ulehčuje práci na sítích, na jejichž spravování mi nezbývá čas. Moc za to vše děkuji."',
        'testimonial3': '„Spolupráce s holkama je jedna radost. Pracují rychle a flexibilně a my můžeme starosti o marketing a propagaci pustit z hlavy."',
        'faq.heading': 'Máte otázky?',
        'faq1.q': 'Musím přesně vědět, co chci?',
        'faq1.a': 'Nemusíte. Stačí nám říct, čím se zabýváte a co chcete dosáhnout. Zbytek vyřešíme společně.',
        'faq2.q': 'Pracujete jen s klienty z Příbrami, nebo i odjinud?',
        'faq2.a': 'Jsme z Příbramska, ale vzdálenost pro nás není překážka. Spolupracujeme s klienty v České republice i v zahraničí a vše zvládneme online. Pokud jste z okolí Příbrami a preferujete osobní setkání, rádi se domluvíme.',
        'faq3.q': 'Proč potřebuji logo nebo jednotný vizuální styl?',
        'faq3.a': 'Protože lidé posuzují firmu očima dřív, než si přečtou jediné slovo. Konzistentní a profesionální vzhled buduje důvěru a důvěra prodává.',
        'faq4.q': 'Postaráte se i o správu mých sociálních sítí?',
        'faq4.a': 'Ano. Můžeme se postarat o tvorbu obsahu, grafiku i pravidelné příspěvky nebo vás naučit, jak to zvládnout samostatně. Záleží na tom, co vám víc vyhovuje.',
        'faq5.q': 'Čím se lišíte od jiné agentury nebo freelancera?',
        'faq5.a': 'Nejsme velká agentura ani sólový freelancer. Ke každému projektu přistupujeme individuálně, spojujeme různé zkušenosti a pohledy, takže výsledek dává smysl z více stran najednou.',
        'faq6.q': 'Co je to brand manuál?',
        'faq6.a': 'Průvodce tím, jak vaše značka vypadá a komunikuje. Barvy, písmo, logo, tón textu. Základ pro každého, kdo chce působit konzistentně a profesionálně.',
        'faq7.q': 'Potřebuji web, když mám sociální sítě?',
        'faq7.a': 'Sociální sítě jsou skvělý nástroj, ale web je váš vlastní prostor na internetu, který nikdo nemůže zavřít ani změnit pravidla. Funguje pro vás 24 hodin denně a zákazníci vás na něm hledají, když chtějí vědět víc. Ideální kombinace je web jako základ a sociální sítě jako cesta k němu.',
        'faq8.q': 'Jak poznám, že mi grafika nebo web skutečně pomáhají?',
        'faq8.a': 'Dobře odvedená práce se projeví tím, že vás lidé začnou vnímat jako profesionály ještě před prvním kontaktem. Zákazníci se vracejí, referují vás dál a vy nemusíte každému znovu vysvětlovat, co děláte. To jsou znaky, že vizuál a obsah pracují za vás.',
        'faq9.q': 'Jak dlouho trvá vytvoření webu?',
        'faq9.a': 'Jednoduchý firemní web vytvoříme do dvou týdnů. Rozsáhlejší weby trvají 3–6 týdnů. Vaši konkrétní situaci probereme na <a href="#contact">konzultaci</a>.',
        'contact.heading': 'Pojďme spolupracovat',
        'contact.subtitle': 'Máte projekt/firmu a hodily by se vám naše služby? Kontaktujte nás.',
        'contact.name.label': 'Jméno a příjmení',
        'contact.name.placeholder': 'Jméno a příjmení',
        'contact.email.label': 'Email',
        'contact.email.placeholder': 'Email',
        'contact.phone.label': 'Telefonní číslo (nepovinné)',
        'contact.phone.placeholder': 'Telefonní číslo (nepovinné)',
        'contact.message.label': 'Popis projektu',
        'contact.message.placeholder': 'Popište svůj projekt, co potřebujete, v jakém termínu…',
        'contact.gdpr': 'Souhlasím se zpracováním osobních údajů dle <a href="gdpr.html" target="_blank" rel="noopener">Zásad ochrany osobních údajů</a>.',
        'contact.submit': 'Poslat zprávu',
        'contact.how.title': 'Jak to funguje',
        'contact.step1': 'Vyplníte formulář',
        'contact.step2': 'Do 48 hodin se ozveme',
        'contact.step3': 'Domluvíme si konzultaci — <em>zdarma, bez závazků</em>',
        'footer.tagline': 'Design, který mluví za Vás.',
        'footer.gdpr': 'Ochrana osobních údajů',
        'exit.label': 'Počkejte!',
        'exit.title': 'Úvodní konzultace<br>je zcela zdarma.',
        'exit.text': 'Probereme váš projekt, odpovíme na všechny dotazy a navrhneme nejlepší řešení.',
        'exit.cta': 'Rezervovat konzultaci',
        'exit.dismiss': 'Ne, děkuji',
        'chat.title': 'Napište nám',
        'chat.submit': 'Odeslat zprávu',
        'chat.fab.aria': 'Otevřít kontaktní formulář',
        'chat.success': '<p style="text-align:center;padding:1rem 0;color:var(--color-dark);font-weight:700;">Zpráva odeslána!<br><span style="font-weight:400;font-size:0.9rem;">Ozveme se do 48 hodin.</span></p>',
        'pdf.modal.aria': 'Náhled PDF dokumentu',
        'pdf.close.aria': 'Zavřít náhled',
    },
    en: {
        'nav.services': 'Services',
        'nav.portfolio': 'Portfolio',
        'nav.about': 'About',
        'nav.references': 'References',
        'nav.faq': 'FAQ',
        'nav.contact': 'Contact',
        'nav.hamburger.aria': 'Open menu',
        'hero.subtitle': 'Creative Studio',
        'hero.text': 'Behind every successful brand stands thoughtful design, great content, and a clear strategy. That’s exactly what we’re here for.',
        'hero.cta': 'Book a Consultation',
        'partners.title': 'We Work With',
        'services.heading': 'Our Services',
        'services.hint.desktop': 'Hover over a card for more information',
        'services.hint.mobile': 'Tap a card for more information',
        'card1.aria': 'Design & Digital',
        'card1.heading': 'Design & Digital',
        'card1.question': 'How to have a website and materials that look professional and polished?',
        'peel.label': 'tap<br>the card',
        'card1.item1': 'Website design & development',
        'card1.item2': 'Social media graphics',
        'card1.item3': 'Print materials',
        'card1.item4': 'Presentations & templates',
        'card2.aria': 'Marketing & Strategy',
        'card2.heading': 'Marketing & Strategy',
        'card2.question': 'How to effectively attract new clients online?',
        'card2.item1': 'Communication strategy',
        'card2.item2': 'Social media management',
        'card2.item3': 'Paid online advertising',
        'card3.aria': 'Consulting, Mentoring & Education',
        'card3.heading': 'Consulting, Mentoring & Education',
        'card3.question': 'Want to take control of your marketing? We’ll teach you exactly what you need.',
        'card3.item1': 'Tailored individual consultations',
        'card3.item2': 'Social media training courses',
        'card4.aria': 'Brand & Identity',
        'card4.heading': 'Brand & Identity',
        'card4.question': 'How to build a brand that’s easily recognisable and trustworthy?',
        'card4.item1': 'Branding & rebranding',
        'card4.item2': 'Logo design',
        'card4.item3': 'Visual identity unification',
        'portfolio.heading': 'Portfolio',
        'portfolio.subtitle': 'Selected projects we’ve worked on',
        'portfolio.branding.title': 'Brands we’ve created visual identities for',
        'portfolio.prev.aria': 'Previous',
        'portfolio.next.aria': 'Next',
        'portfolio.pdf1.aria': 'View PDF preview, portfolio 1',
        'portfolio.pdf2.aria': 'View PDF preview, portfolio 2',
        'portfolio.pdf3.aria': 'View PDF preview, portfolio 3',
        'portfolio.grafika.title': 'Digital Graphics & Social Media',
        'portfolio.grafika.aria': 'Digital graphics portfolio samples',
        'portfolio.web.title': 'Websites we brought to life',
        'portfolio.web.aria': 'Website portfolio samples',
        'portfolio.web.prev.aria': 'Previous website',
        'portfolio.web.next.aria': 'Next website',
        'portfolio.print.title': 'Print Materials',
        'portfolio.print.aria': 'Print materials portfolio samples',
        'print.label1': 'Drinks Menu – La Familia',
        'print.label2': 'B4 Fashion – package design',
        'print.label3': 'Wedding Invitation',
        'print.label4': 'Dubno Village Newsletter',
        'print.label5': 'Gift Voucher – Fyzio Body Profik',
        'print.label6': 'Business Card – P2 Projekt',
        'print.btn1.aria': 'View drinks menu – La Familia',
        'print.btn2.aria': 'View B4 Fashion – package design',
        'print.btn3.aria': 'View wedding invitation',
        'print.btn4.aria': 'View Dubno village newsletter',
        'print.btn5.aria': 'View gift voucher – Fyzio Body Profik',
        'print.btn6.aria': 'View business card – P2 Projekt',
        'lightbox.aria': 'Image preview',
        'lightbox.close.aria': 'Close preview',
        'about.heading': 'About Us',
        'about.p1': 'Black Mango was born from a need to <strong>do things differently</strong>. We combined experience from different fields and created a space where <strong>design, content, and our approach to clients</strong> go hand in hand.',
        'about.p2': 'We approach every project <strong>individually</strong>. We want to understand what you truly need, and then handle it carefully and thoughtfully. <strong>We lead the project</strong>, keep track of deadlines, and what we agree on, we deliver. <strong>You can focus on your business.</strong>',
        'about.p3': 'Good collaboration, in our view, works both ways. We adapt our work to your needs, but when something doesn’t make sense to us, <strong>we say it straight</strong>. Not to be right, but because <strong>the outcome matters to us.</strong> For our clients, we are a <strong>STRATEGIC PARTNER</strong>.',
        'about.p4': '<strong>WE BRING SOMETHING EXTRA.</strong> We guide you from the first idea all the way to the result you can see. A paid invoice is not the end of our collaboration. <strong>We root for our clients, follow their progress, and are here when they need advice or a push forward.</strong>',
        'about.value1': '9 years of experience',
        'about.value2': 'individual approach',
        'about.value3': 'long-term partnership',
        'about.value4': 'meeting deadlines',
        'references.heading': 'References',
        'testimonial1': '“Working with Black Mango is truly wonderful. From our very first meeting, I knew our collaboration would be fruitful. Thanks to their skills, flexibility, responsiveness and dedication, my social media now features posts that really connect with clients. Their work makes managing my channels so much easier when I have no time to do it myself. I’m very grateful for everything.”',
        'testimonial3': '“Working with the girls is an absolute pleasure. They work quickly and flexibly, and we can completely let go of our marketing and promotion worries.”',
        'faq.heading': 'Got Questions?',
        'faq1.q': 'Do I need to know exactly what I want?',
        'faq1.a': 'Not at all. Just tell us what you do and what you want to achieve. We’ll figure out the rest together.',
        'faq2.q': 'Do you only work with clients from Příbram, or elsewhere too?',
        'faq2.a': 'We’re based near Příbram, but distance is no obstacle. We work with clients across the Czech Republic and internationally — everything can be handled online. If you’re local and prefer to meet in person, we’re happy to arrange that.',
        'faq3.q': 'Why do I need a logo or a consistent visual style?',
        'faq3.a': 'Because people judge a business visually before they read a single word. A consistent, professional look builds trust — and trust sells.',
        'faq4.q': 'Can you also manage my social media?',
        'faq4.a': 'Yes. We can take care of content creation, graphics, and regular posts — or teach you how to handle it yourself. It depends on what works best for you.',
        'faq5.q': 'How are you different from an agency or a freelancer?',
        'faq5.a': 'We’re neither a large agency nor a solo freelancer. We approach every project individually, combining different experiences and perspectives, so the result makes sense from multiple angles at once.',
        'faq6.q': 'What is a brand manual?',
        'faq6.a': 'A guide to how your brand looks and communicates — colours, typography, logo, tone of voice. The foundation for anyone who wants to come across as consistent and professional.',
        'faq7.q': 'Do I need a website if I have social media?',
        'faq7.a': 'Social media is a great tool, but a website is your own space on the internet — no one can shut it down or change the rules. It works for you 24 hours a day, and customers look for you there when they want to know more. The ideal combination: a website as the foundation, social media as the path to it.',
        'faq8.q': 'How will I know if the graphics or website are actually helping me?',
        'faq8.a': 'Good work shows when people start perceiving you as a professional before the first contact. Customers return, they refer you to others, and you no longer need to explain what you do to everyone. Those are the signs that your visuals and content are working for you.',
        'faq9.q': 'How long does it take to build a website?',
        'faq9.a': 'A simple business website takes up to two weeks. More complex sites take 3–6 weeks. We’ll discuss your specific situation at a <a href="#contact">consultation</a>.',
        'contact.heading': 'Let’s Work Together',
        'contact.subtitle': 'Got a project or a business that could use our services? Get in touch.',
        'contact.name.label': 'Full name',
        'contact.name.placeholder': 'Full name',
        'contact.email.label': 'Email',
        'contact.email.placeholder': 'Email',
        'contact.phone.label': 'Phone number (optional)',
        'contact.phone.placeholder': 'Phone number (optional)',
        'contact.message.label': 'Project description',
        'contact.message.placeholder': 'Describe your project, what you need, and your timeline…',
        'contact.gdpr': 'I agree to the processing of personal data in accordance with the <a href="gdpr.html" target="_blank" rel="noopener">Privacy Policy</a>.',
        'contact.submit': 'Send Message',
        'contact.how.title': 'How It Works',
        'contact.step1': 'Fill in the form',
        'contact.step2': 'We’ll get back to you within 48 hours',
        'contact.step3': 'We’ll arrange a consultation — <em>free, no strings attached</em>',
        'footer.tagline': 'Design that speaks for you.',
        'footer.gdpr': 'Privacy Policy',
        'exit.label': 'Wait!',
        'exit.title': 'Initial consultation<br>is completely free.',
        'exit.text': 'We’ll discuss your project, answer all your questions, and suggest the best solution.',
        'exit.cta': 'Book a Consultation',
        'exit.dismiss': 'No, thank you',
        'chat.title': 'Write to Us',
        'chat.submit': 'Send Message',
        'chat.fab.aria': 'Open contact form',
        'chat.success': '<p style="text-align:center;padding:1rem 0;color:var(--color-dark);font-weight:700;">Message sent!<br><span style="font-weight:400;font-size:0.9rem;">We’ll get back to you within 48 hours.</span></p>',
        'pdf.modal.aria': 'PDF document preview',
        'pdf.close.aria': 'Close preview',
    }
};

/* ===========================
   LANGUAGE SWITCHER
   =========================== */
(function () {
    const STORAGE_KEY = 'bm_lang';

    // Detect which page we're on from the URL path
    const isEnPage = window.location.pathname.includes('/en');

    // On the EN page localStorage is already set by the inline script in <head>.
    // On the CZ page, respect stored preference or default to 'cz'.
    let currentLang = isEnPage ? 'en' : (localStorage.getItem(STORAGE_KEY) || 'cz');

    function applyLang(lang) {
        const t = translations[lang];
        if (!t) return;

        document.documentElement.lang = lang === 'en' ? 'en' : 'cs';

        document.querySelectorAll('[data-i18n]').forEach(el => {
            const key = el.dataset.i18n;
            if (t[key] !== undefined) el.textContent = t[key];
        });

        document.querySelectorAll('[data-i18n-html]').forEach(el => {
            const key = el.dataset.i18nHtml;
            if (t[key] !== undefined) el.innerHTML = t[key];
        });

        document.querySelectorAll('[data-i18n-placeholder]').forEach(el => {
            const key = el.dataset.i18nPlaceholder;
            if (t[key] !== undefined) el.placeholder = t[key];
        });

        document.querySelectorAll('[data-i18n-aria]').forEach(el => {
            const key = el.dataset.i18nAria;
            if (t[key] !== undefined) el.setAttribute('aria-label', t[key]);
        });

        document.querySelectorAll('.lang-btn').forEach(btn => {
            const active = btn.dataset.lang === lang;
            btn.classList.toggle('lang-btn--active', active);
            btn.setAttribute('aria-pressed', String(active));
        });

        currentLang = lang;
        localStorage.setItem(STORAGE_KEY, lang);
    }

    document.querySelectorAll('.lang-btn').forEach(btn => {
        btn.addEventListener('click', () => {
            const target = btn.dataset.lang;

            // Navigate between separate URLs for proper SEO indexing
            if (target === 'en' && !isEnPage) {
                localStorage.setItem(STORAGE_KEY, 'en');
                window.location.href = '/en/';
                return;
            }
            if (target === 'cz' && isEnPage) {
                localStorage.setItem(STORAGE_KEY, 'cz');
                window.location.href = '/';
                return;
            }

            // Already on the right page — just apply translations
            applyLang(target);
        });
    });

    applyLang(currentLang);

    window.getCurrentLang = () => currentLang;
    window.getLangKey = (key) => (translations[currentLang] || translations.cz)[key];
}());

/* ===========================
   FLIP CARDS – click toggle (mobile & keyboard)
   =========================== */
document.querySelectorAll('.flip-card').forEach(card => {
    card.addEventListener('click', function () {
        this.classList.toggle('is-flipped');
    });

    card.addEventListener('keydown', function (e) {
        if (e.key === 'Enter' || e.key === ' ') {
            e.preventDefault();
            this.classList.toggle('is-flipped');
        }
    });

    card.addEventListener('mouseleave', function () {
        this.classList.remove('is-flipped');
    });
});

/* ===========================
   FORM HANDLING
   =========================== */
document.getElementById('contactForm').addEventListener('submit', function(e) {
    e.preventDefault();

    const name = this.querySelector('input[type="text"]').value;
    const email = this.querySelector('input[type="email"]').value;
    const message = this.querySelector('textarea').value;

    if (!name || !email || !message) {
        alert(window.getLangKey('contact.submit') === 'Send Message'
            ? 'Please fill in all required fields.'
            : 'Prosím, vyplňte všechna povinná pole.');
        return;
    }

    const lang = window.getCurrentLang();
    if (lang === 'en') {
        alert(`Thank you, ${name}! We have received your message and will get back to you soon.`);
    } else {
        alert(`Děkujeme, ${name}! Vaši zprávu jsme obdrželi a brzy se vám ozveme.`);
    }

    this.reset();
});

/* ===========================
   SMOOTH SCROLL ENHANCEMENT
   =========================== */
document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    anchor.addEventListener('click', function (e) {
        e.preventDefault();
        const target = document.querySelector(this.getAttribute('href'));
        if (target) {
            target.scrollIntoView({
                behavior: 'smooth',
                block: 'start'
            });
        }
    });
});

/* ===========================
   NAVBAR ANIMATION ON SCROLL
   =========================== */
let lastScrollTop = 0;
const navbar = document.querySelector('.navbar');

window.addEventListener('scroll', function() {
    let currentScroll = window.pageYOffset || document.documentElement.scrollTop;

    if (currentScroll > 100) {
        navbar.style.boxShadow = '0 2px 8px rgba(12, 35, 32, 0.1)';
    } else {
        navbar.style.boxShadow = 'none';
    }

    lastScrollTop = currentScroll <= 0 ? 0 : currentScroll;
});

/* ===========================
   FAQ ACCORDION
   =========================== */
document.querySelectorAll('.faq-question').forEach(button => {
    button.addEventListener('click', function () {
        const answer = this.nextElementSibling;
        const isOpen = this.getAttribute('aria-expanded') === 'true';

        document.querySelectorAll('.faq-question').forEach(btn => {
            btn.setAttribute('aria-expanded', 'false');
            btn.nextElementSibling.classList.remove('is-open');
        });

        if (!isOpen) {
            this.setAttribute('aria-expanded', 'true');
            answer.classList.add('is-open');
        }
    });
});

/* ===========================
    SCROLL REVEAL ANIMATION
    =========================== */
const revealElements = document.querySelectorAll('.flip-card, .portfolio-carousel-block, .testimonial');

const revealOnScroll = () => {
    revealElements.forEach(element => {
        const elementTop = element.getBoundingClientRect().top;
        const elementBottom = element.getBoundingClientRect().bottom;

        if (elementTop < window.innerHeight && elementBottom > 0) {
            element.style.opacity = '1';
            element.style.transform = 'translateY(0)';
        }
    });
};

revealElements.forEach(element => {
    element.style.opacity = '0';
    element.style.transform = 'translateY(20px)';
    element.style.transition = 'opacity 0.6s ease, transform 0.6s ease';
});

window.addEventListener('scroll', revealOnScroll);
window.addEventListener('load', revealOnScroll);
revealOnScroll();

/* ===========================
   ACTIVE NAV LINK ON SCROLL
   =========================== */
window.addEventListener('scroll', () => {
    let current = '';

    const sections = document.querySelectorAll('section[id]');

    sections.forEach(section => {
        const sectionTop = section.offsetTop;
        const sectionHeight = section.clientHeight;

        if (pageYOffset >= (sectionTop - 200)) {
            current = section.getAttribute('id');
        }
    });

    document.querySelectorAll('.nav-menu a').forEach(link => {
        link.classList.remove('active');
        if (link.getAttribute('href').slice(1) === current) {
            link.style.color = '#D6C3A3';
        } else {
            link.style.color = '#0C2320';
        }
    });
});

/* ===========================
   PORTFOLIO CAROUSEL + PDF PREVIEW
   =========================== */
const pdfModal = document.getElementById('pdfPreviewModal');
const pdfFrame = document.getElementById('pdfPreviewFrame');
const pdfClose = document.getElementById('pdfPreviewClose');

function buildPdfPreviewUrl(path) {
    return `${encodeURI(path)}#toolbar=0&navpanes=0&scrollbar=0&view=FitH`;
}

function openPdfPreview(path) {
    if (!pdfModal || !pdfFrame) return;
    pdfFrame.src = buildPdfPreviewUrl(path);
    pdfModal.classList.add('is-open');
    pdfModal.setAttribute('aria-hidden', 'false');
    document.body.style.overflow = 'hidden';
}

function closePdfPreview() {
    if (!pdfModal || !pdfFrame) return;
    pdfModal.classList.remove('is-open');
    pdfModal.setAttribute('aria-hidden', 'true');
    pdfFrame.src = 'about:blank';
    document.body.style.overflow = '';
}

if (pdfModal && pdfFrame) {
    document.querySelectorAll('.portfolio-slide[data-pdf]').forEach(slide => {
        slide.addEventListener('click', () => openPdfPreview(slide.dataset.pdf));
    });

    if (pdfClose) {
        pdfClose.addEventListener('click', closePdfPreview);
    }

    pdfModal.querySelectorAll('[data-close-pdf="true"]').forEach(node => {
        node.addEventListener('click', closePdfPreview);
    });

    document.addEventListener('keydown', (e) => {
        if (e.key === 'Escape' && pdfModal.classList.contains('is-open')) {
            closePdfPreview();
        }
    });

    pdfModal.addEventListener('contextmenu', (e) => e.preventDefault());
}

document.querySelectorAll('.portfolio-carousel').forEach(carousel => {
    const track = carousel.querySelector('.portfolio-carousel-track');
    const slides = carousel.querySelectorAll('.portfolio-slide');
    const prev = carousel.querySelector('.carousel-btn-prev');
    const next = carousel.querySelector('.carousel-btn-next');
    const dots = carousel.parentElement.querySelectorAll('.carousel-dot');

    if (!track || slides.length === 0) return;

    let currentIndex = 0;

    const updateCarousel = () => {
        track.style.transform = `translateX(-${currentIndex * 100}%)`;
        dots.forEach((dot, idx) => {
            dot.classList.toggle('is-active', idx === currentIndex);
        });
    };

    if (prev) {
        prev.addEventListener('click', () => {
            currentIndex = (currentIndex - 1 + slides.length) % slides.length;
            updateCarousel();
        });
    }

    if (next) {
        next.addEventListener('click', () => {
            currentIndex = (currentIndex + 1) % slides.length;
            updateCarousel();
        });
    }

    dots.forEach(dot => {
        dot.addEventListener('click', () => {
            const idx = Number(dot.dataset.slide);
            if (!Number.isNaN(idx) && idx >= 0 && idx < slides.length) {
                currentIndex = idx;
                updateCarousel();
            }
        });
    });

    updateCarousel();
});

/* ===========================
   PARTNERS MARQUEE (SEAMLESS LOOP)
   =========================== */
function initPartnersMarquee() {
    const reducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)');

    document.querySelectorAll('.partners-marquee').forEach(marquee => {
        const track = marquee.querySelector('.partners-marquee-track');
        const groups = track ? track.querySelectorAll('.partners-marquee-group') : [];

        if (!track || groups.length < 2) return;

        let loopDistance = 0;
        let offset = 0;
        let lastTimestamp = 0;
        let rafId = null;
        let isPaused = false;
        const speedPxPerSecond = 34;

        const applyTransform = () => {
            track.style.transform = `translate3d(${-offset}px, 0, 0)`;
        };

        const recalc = () => {
            const nextDistance = groups[0].getBoundingClientRect().width;
            if (nextDistance > 0) {
                loopDistance = nextDistance;
            }
            if (loopDistance > 0) {
                offset = offset % loopDistance;
            }
            applyTransform();
        };

        const step = (timestamp) => {
            if (!lastTimestamp) {
                lastTimestamp = timestamp;
            }

            const delta = (timestamp - lastTimestamp) / 1000;
            lastTimestamp = timestamp;

            if (!isPaused && !reducedMotion.matches && loopDistance > 0) {
                offset += speedPxPerSecond * delta;
                if (offset >= loopDistance) {
                    offset -= loopDistance;
                }
                applyTransform();
            }

            rafId = window.requestAnimationFrame(step);
        };

        const pause = () => { isPaused = true; };
        const resume = () => { isPaused = false; };

        marquee.addEventListener('mouseenter', pause);
        marquee.addEventListener('mouseleave', resume);
        marquee.addEventListener('focusin', pause);
        marquee.addEventListener('focusout', resume);

        window.addEventListener('resize', recalc);

        if (document.fonts && document.fonts.ready) {
            document.fonts.ready.then(recalc);
        }

        if (reducedMotion.addEventListener) {
            reducedMotion.addEventListener('change', () => {
                if (reducedMotion.matches) {
                    offset = 0;
                    applyTransform();
                }
                lastTimestamp = 0;
            });
        }

        recalc();

        if (!rafId) {
            rafId = window.requestAnimationFrame(step);
        }
    });
}

initPartnersMarquee();

/* ===========================
   PORTFOLIO WEB CAROUSEL (SEAMLESS LOOP)
   =========================== */
function initPortfolioWebCarousel() {
    const reducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)');

    document.querySelectorAll('.portfolio-web-carousel').forEach(carousel => {
        const track = carousel.querySelector('.portfolio-web-carousel-track');
        const groups = track ? track.querySelectorAll('.portfolio-web-carousel-group') : [];
        const prevButton = carousel.querySelector('.portfolio-web-carousel-btn-prev');
        const nextButton = carousel.querySelector('.portfolio-web-carousel-btn-next');

        if (!track || groups.length < 2) return;

        let loopDistance = 0;
        let offset = 0;
        let lastTimestamp = 0;
        let rafId = null;
        let isPaused = false;
        const speedPxPerSecond = 26;

        const applyTransform = () => {
            track.style.transform = `translate3d(${-offset}px, 0, 0)`;
        };

        const recalc = () => {
            const nextDistance = groups[0].getBoundingClientRect().width;
            if (nextDistance > 0) {
                loopDistance = nextDistance;
            }
            if (loopDistance > 0) {
                offset = offset % loopDistance;
            }
            applyTransform();
        };

        const getStepDistance = () => {
            const firstCard = groups[0].querySelector('.portfolio-web-card');
            if (!firstCard) return 0;

            const cardWidth = firstCard.getBoundingClientRect().width;
            const groupStyles = window.getComputedStyle(groups[0]);
            const gap = parseFloat(groupStyles.columnGap || groupStyles.gap || '0') || 0;

            return cardWidth + gap;
        };

        const nudge = (direction) => {
            const stepDistance = getStepDistance();
            if (!stepDistance || !loopDistance) return;

            isPaused = true;
            offset += direction * stepDistance;
            offset = ((offset % loopDistance) + loopDistance) % loopDistance;
            applyTransform();

            window.clearTimeout(carousel._resumeTimeout);
            carousel._resumeTimeout = window.setTimeout(() => {
                isPaused = false;
            }, 1400);
        };

        const step = (timestamp) => {
            if (!lastTimestamp) {
                lastTimestamp = timestamp;
            }

            const delta = (timestamp - lastTimestamp) / 1000;
            lastTimestamp = timestamp;

            if (!isPaused && !reducedMotion.matches && loopDistance > 0) {
                offset += speedPxPerSecond * delta;
                if (offset >= loopDistance) {
                    offset -= loopDistance;
                }
                applyTransform();
            }

            rafId = window.requestAnimationFrame(step);
        };

        const pause = () => { isPaused = true; };
        const resume = () => { isPaused = false; };

        carousel.addEventListener('mouseenter', pause);
        carousel.addEventListener('mouseleave', resume);
        carousel.addEventListener('focusin', pause);
        carousel.addEventListener('focusout', resume);

        if (prevButton) {
            prevButton.addEventListener('click', () => nudge(-1));
        }

        if (nextButton) {
            nextButton.addEventListener('click', () => nudge(1));
        }

        window.addEventListener('resize', recalc);

        if (document.fonts && document.fonts.ready) {
            document.fonts.ready.then(recalc);
        }

        if (reducedMotion.addEventListener) {
            reducedMotion.addEventListener('change', () => {
                if (reducedMotion.matches) {
                    offset = 0;
                    applyTransform();
                }
                lastTimestamp = 0;
            });
        }

        recalc();

        if (!rafId) {
            rafId = window.requestAnimationFrame(step);
        }
    });
}

initPortfolioWebCarousel();

/* ===========================
   PORTFOLIO GRAFIKA CAROUSEL (SEAMLESS LOOP)
   =========================== */
function initPortfolioGrafikaCarousel() {
    const reducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)');

    document.querySelectorAll('.portfolio-grafika-carousel').forEach(carousel => {
        const track = carousel.querySelector('.portfolio-grafika-carousel-track');
        const groups = track ? track.querySelectorAll('.portfolio-grafika-carousel-group') : [];
        const prevButton = carousel.querySelector('.portfolio-web-carousel-btn-prev');
        const nextButton = carousel.querySelector('.portfolio-web-carousel-btn-next');

        if (!track || groups.length < 2) return;

        let loopDistance = 0;
        let offset = 0;
        let lastTimestamp = 0;
        let rafId = null;
        let isPaused = false;
        const speedPxPerSecond = 26;

        const applyTransform = () => {
            track.style.transform = `translate3d(${-offset}px, 0, 0)`;
        };

        const recalc = () => {
            const nextDistance = groups[0].getBoundingClientRect().width;
            if (nextDistance > 0) loopDistance = nextDistance;
            if (loopDistance > 0) offset = offset % loopDistance;
            applyTransform();
        };

        const getStepDistance = () => {
            const firstCard = groups[0].querySelector('.portfolio-grafika-card');
            if (!firstCard) return 0;
            const cardWidth = firstCard.getBoundingClientRect().width;
            const gap = parseFloat(window.getComputedStyle(groups[0]).columnGap || '0') || 0;
            return cardWidth + gap;
        };

        const nudge = (direction) => {
            const stepDistance = getStepDistance();
            if (!stepDistance || !loopDistance) return;
            isPaused = true;
            offset += direction * stepDistance;
            offset = ((offset % loopDistance) + loopDistance) % loopDistance;
            applyTransform();
            window.clearTimeout(carousel._resumeTimeout);
            carousel._resumeTimeout = window.setTimeout(() => { isPaused = false; }, 1400);
        };

        const step = (timestamp) => {
            if (!lastTimestamp) lastTimestamp = timestamp;
            const delta = (timestamp - lastTimestamp) / 1000;
            lastTimestamp = timestamp;
            if (!isPaused && !reducedMotion.matches && loopDistance > 0) {
                offset += speedPxPerSecond * delta;
                if (offset >= loopDistance) offset -= loopDistance;
                applyTransform();
            }
            rafId = window.requestAnimationFrame(step);
        };

        carousel.addEventListener('mouseenter', () => { isPaused = true; });
        carousel.addEventListener('mouseleave', () => { isPaused = false; });
        carousel.addEventListener('focusin', () => { isPaused = true; });
        carousel.addEventListener('focusout', () => { isPaused = false; });

        if (prevButton) prevButton.addEventListener('click', () => nudge(-1));
        if (nextButton) nextButton.addEventListener('click', () => nudge(1));

        window.addEventListener('resize', recalc);
        if (document.fonts && document.fonts.ready) document.fonts.ready.then(recalc);

        recalc();
        if (!rafId) rafId = window.requestAnimationFrame(step);
    });
}

initPortfolioGrafikaCarousel();

/* ===========================
   TISKOVINY LIGHTBOX
   =========================== */
(function () {
    const modal = document.getElementById('lightboxModal');
    const img = document.getElementById('lightboxImg');
    const closeBtn = document.getElementById('lightboxClose');

    if (!modal || !img) return;

    function openLightbox(src, alt) {
        img.src = src;
        img.alt = alt || '';
        modal.classList.add('is-open');
        modal.setAttribute('aria-hidden', 'false');
        document.body.style.overflow = 'hidden';
        closeBtn && closeBtn.focus();
    }

    function closeLightbox() {
        modal.classList.remove('is-open');
        modal.setAttribute('aria-hidden', 'true');
        img.src = '';
        img.alt = '';
        document.body.style.overflow = '';
    }

    document.querySelectorAll('[data-lightbox-src]').forEach(btn => {
        btn.addEventListener('click', () => {
            openLightbox(btn.dataset.lightboxSrc, btn.dataset.lightboxAlt);
        });
    });

    if (closeBtn) closeBtn.addEventListener('click', closeLightbox);

    modal.querySelectorAll('[data-close-lightbox="true"]').forEach(el => {
        el.addEventListener('click', closeLightbox);
    });

    document.addEventListener('keydown', (e) => {
        if (e.key === 'Escape' && modal.classList.contains('is-open')) closeLightbox();
    });
}());

/* ===========================
   FLOATING CHAT WIDGET
   =========================== */
(function () {
    const widget = document.getElementById('chatWidget');
    const fab = document.getElementById('chatFab');
    const panel = document.getElementById('chatPanel');
    const closeBtn = document.getElementById('chatPanelClose');
    const form = document.getElementById('chatForm');

    if (!widget || !fab || !panel) return;

    function openChat() {
        widget.classList.add('is-open');
        panel.setAttribute('aria-hidden', 'false');
        fab.setAttribute('aria-expanded', 'true');
        const first = panel.querySelector('input, textarea, button');
        if (first) first.focus();
    }

    function closeChat() {
        widget.classList.remove('is-open');
        panel.setAttribute('aria-hidden', 'true');
        fab.setAttribute('aria-expanded', 'false');
        fab.focus();
    }

    fab.addEventListener('click', () => {
        widget.classList.contains('is-open') ? closeChat() : openChat();
    });

    if (closeBtn) closeBtn.addEventListener('click', closeChat);

    document.addEventListener('keydown', (e) => {
        if (e.key === 'Escape' && widget.classList.contains('is-open')) closeChat();
    });

    if (form) {
        form.addEventListener('submit', (e) => {
            e.preventDefault();
            const successHtml = window.getLangKey ? window.getLangKey('chat.success') : translations.cz['chat.success'];
            form.innerHTML = successHtml;
        });
    }
}());

/* ===========================
   EXIT INTENT POPUP
   =========================== */
(function () {
    const popup = document.getElementById('exitPopup');
    const backdrop = document.getElementById('exitPopupBackdrop');
    const closeBtn = document.getElementById('exitPopupClose');
    const dismissBtn = document.getElementById('exitPopupDismiss');
    const ctaBtn = document.getElementById('exitPopupCta');

    if (!popup) return;

    const SESSION_KEY = 'bm_exit_popup_shown';
    let readyToShow = false;

    if (sessionStorage.getItem(SESSION_KEY)) return;

    window.setTimeout(() => { readyToShow = true; }, 4000);

    function openPopup() {
        if (!readyToShow || sessionStorage.getItem(SESSION_KEY)) return;
        sessionStorage.setItem(SESSION_KEY, '1');
        popup.classList.add('is-open');
        popup.setAttribute('aria-hidden', 'false');
        document.body.style.overflow = 'hidden';
        closeBtn && closeBtn.focus();
    }

    function closePopup() {
        popup.classList.remove('is-open');
        popup.setAttribute('aria-hidden', 'true');
        document.body.style.overflow = '';
    }

    document.addEventListener('mouseleave', (e) => {
        if (e.clientY <= 0) openPopup();
    });

    if (backdrop) backdrop.addEventListener('click', closePopup);
    if (closeBtn) closeBtn.addEventListener('click', closePopup);
    if (dismissBtn) dismissBtn.addEventListener('click', closePopup);

    if (ctaBtn) {
        ctaBtn.addEventListener('click', (e) => {
            e.preventDefault();
            closePopup();
            const target = document.querySelector('#contact');
            if (target) target.scrollIntoView({ behavior: 'smooth', block: 'start' });
        });
    }

    document.addEventListener('keydown', (e) => {
        if (e.key === 'Escape' && popup.classList.contains('is-open')) closePopup();
    });
}());

/* ===========================
   MOBILE MENU (HAMBURGER)
   =========================== */
(function () {
    const btn = document.getElementById('navHamburger');
    const menu = document.getElementById('navMenu');
    if (!btn || !menu) return;

    function closeMenu() {
        btn.classList.remove('is-open');
        menu.classList.remove('is-open');
        btn.setAttribute('aria-expanded', 'false');
    }

    btn.addEventListener('click', () => {
        const open = btn.classList.toggle('is-open');
        menu.classList.toggle('is-open', open);
        btn.setAttribute('aria-expanded', String(open));
    });

    menu.querySelectorAll('a').forEach(link => {
        link.addEventListener('click', closeMenu);
    });

    document.addEventListener('click', (e) => {
        if (!btn.contains(e.target) && !menu.contains(e.target)) closeMenu();
    });

    document.addEventListener('keydown', (e) => {
        if (e.key === 'Escape') closeMenu();
    });
}());

/* ===========================
   BRANDING CAROUSEL – PREV / NEXT ARROWS (mobile)
   =========================== */
(function () {
    const grid = document.getElementById('brandingGrid');
    const prev = document.getElementById('brandingPrev');
    const next = document.getElementById('brandingNext');
    if (!grid || !prev || !next) return;

    const scrollBy = (dir) => {
        const slideWidth = grid.querySelector('.portfolio-slide')
            ? grid.querySelector('.portfolio-slide').offsetWidth
            : grid.clientWidth;
        grid.scrollBy({ left: dir * slideWidth, behavior: 'smooth' });
    };

    prev.addEventListener('click', () => scrollBy(-1));
    next.addEventListener('click', () => scrollBy(1));
}());

/* ===========================
   PAGE LOAD ANIMATION
   =========================== */
window.addEventListener('load', () => {
    document.body.style.opacity = '1';
    document.body.style.animation = 'fadeIn 0.5s ease';
});

document.body.style.opacity = '0.95';
